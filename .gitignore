# credentials — never commit
.env

# python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/
*.whl

# range artefacts
*.db
*.db-journal
*.sqlite
keys/

# one-off campaign output. The committed matrix lives in results/ itself.
results/scratch/

# editor and tooling
.vscode/
.idea/
.claude/

# os
Thumbs.db
.DS_Store
dist/

# Word's lock file, present whenever the walkthrough is open for reading.
# Untracked but not ignored, it rode along with any `git add -A` — and a
# committed ~$ file tells everyone who clones that the document is locked by
# someone who left.
~$*
