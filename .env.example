# Copy to .env and fill in. .env is gitignored and never committed.
# Not needed for Sessions 1-3, or to run the demo / reproduce committed results.
ANTHROPIC_API_KEY=sk-ant-...

# replay = no LLM calls, serves committed results. live = local dev only.
PRAMAN_MODE=live
